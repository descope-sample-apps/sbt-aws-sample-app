import { DlqDestinationConfig, IEventSourceDlq, IEventSourceMapping, IFunction } from 'aws-cdk-lib/aws-lambdalambdalambdalambda';
import * as sns from 'aws-cdk-lib/aws-snsws-snsws-snsws-sns';
/**
 * An SNS dead letter queue destination configuration for a Lambda event source
 */
export declare class SnsDlq implements IEventSourceDlq {
    private readonly topic;
    constructor(topic: sns.ITopic);
    /**
     * Returns a destination configuration for the DLQ
     */
    bind(_target: IEventSourceMapping, targetHandler: IFunction): DlqDestinationConfig;
}
