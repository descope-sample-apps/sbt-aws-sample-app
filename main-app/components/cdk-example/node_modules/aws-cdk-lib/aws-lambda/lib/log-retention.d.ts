import { Construct } from 'constructs';
import * as logs from 'aws-cdk-lib/aws-logss-logs';
/**
 * Retry options for all AWS API calls.
 */
export interface LogRetentionRetryOptions extends logs.LogRetentionRetryOptions {
}
